import React from 'react';
import {BasicComponentProps} from "@typings";
import Card from "@components/card/Card";
import Default from '@stories/card/Default.story';

export default {
    title: 'Card',
    component: Card,
    argTypes: {},
};

export {
    Default
}