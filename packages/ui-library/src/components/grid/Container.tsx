import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
    padding: 0 1rem;

`;

interface Props {

}

const Container: React.FunctionComponent<Props> = (props: Props): React.ReactElement | null => {
    return (
        <Wrapper>

        </Wrapper>
    )
}

export default Container;