import ThemeTransitionInterface from "@theme/interfaces/ThemeTransitionInterface";

const ThemeTransition: ThemeTransitionInterface = {
    elevation: "all .3s cubic-bezier(.25, .8, .25, 1)"
}

export default ThemeTransition;