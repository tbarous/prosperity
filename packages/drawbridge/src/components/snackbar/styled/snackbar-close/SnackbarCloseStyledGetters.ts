import {SnackbarCloseStyledProps} from "@components/snackbar/styled/snackbar-close/SnackbarCloseStyled";

const SnackbarCloseStyledGetters = {
    Display: (props: SnackbarCloseStyledProps) => props.theme.display.flex,
}

export default SnackbarCloseStyledGetters;